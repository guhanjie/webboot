<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mongo="http://www.springframework.org/schema/data/mongo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd">
	<mongo:mongo id="mongo" host="${mongo.host:172.16.233.242}" port="${mongo.port:27017}">
		<mongo:options 
				connections-per-host="${mongo.connectionsPerHost:20}"
				threads-allowed-to-block-for-connection-multiplier="${mongo.threadsAllowedToBlockForConnectionMultiplier:4}"
				connect-timeout="${mongo.connectTimeout:15000}" 
				max-wait-time="${mongo.maxWaitTime:10000}"
				auto-connect-retry="${mongo.autoConnectRetry:true}"
				socket-keep-alive="${mongo.socketKeepAlive:true}" 
				socket-timeout="${mongo.socketTimeout:60000}"
				slave-ok="${mongo.slaveOk:true}" 
				write-number="1" 
				write-timeout="0"
				write-fsync="false" />
	</mongo:mongo>
	<mongo:db-factory id="mongoDbFactory" dbname="database" mongo-ref="mongo" />
	<bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg name="mongo" ref="mongo" />
		<constructor-arg name="databaseName" value="${mongo.dbname:test}" />
		<constructor-arg name="userCredentials">
			<bean class="org.springframework.data.authentication.UserCredentials">
				<constructor-arg name="username" value="${mongo.username:}" />
				<constructor-arg name="password" value="${mongo.password:}" />
			</bean>
		</constructor-arg>
		<property name="writeResultChecking" value="${mongo.writeResultChecking:NONE}" />
	</bean>
	<!-- 设置Spring为这些接口产生Proxy实例 -->
	<mongo:repositories base-package="com.guhanjie.mongo.repository" />
</beans>